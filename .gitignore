# Исключаем системные файлы
.DS_Store
Thumbs.db

# Исключаем файлы окружения
.env
.env.local
.env.production
*.pem

# Исключаем node_modules (если фронтенд на React/Vue)
node_modules/
npm-debug.log
yarn.lock
pnpm-lock.yaml

# Исключаем скомпилированные файлы и кеш
dist/
build/
.cache/
*.log
logs/

# Исключаем зависимости Go
bin/
pkg/
vendor/
coverage.out

# Исключаем Docker-артефакты
docker-compose.override.yml
docker-compose.dev.yml
*.tar
Dockerfile.old

# Исключаем файлы CI/CD (если они не должны быть в репозитории)
.github/
.idea/
.vscode/

# Исключаем временные файлы
*.swp
*.tmp
*.bak

# Исключаем кеш Next.js (если используется)
.next/
out/
